# Linux System Resource Monitor

A Bash script that monitors basic system resources on a Linux system.

## Features

- Displays CPU usage
- Displays memory usage
- Displays disk usage
- Shows the top 5 CPU-consuming processes
- Warns when CPU usage is above 80%
- Warns when memory usage is above 80%

## Usage

Make the script executable:

```bash
chmod u+x monitor.sh
